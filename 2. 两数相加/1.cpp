#include<iostream>
#include<iterator>
#include<list>
using namespace std;
struct ListNode {
  int val;  //当前结点的值
  ListNode* next;  //指向下一个结点的指针
  ListNode(int x) : val(x), next(NULL) {}  //构造函数，初始化当前结点值为x,指针为空
};
class Solution {
 public:
  ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {

  }
};

int mian() { 
    ListNode* L1;
    ListNode* L2;
  
    return 0; 
}